[package]
name        = "diarilog-crypto"
description = "Key derivation and envelope encryption primitives for diarilog E2EE (RFC 011)."
version.workspace       = true
edition.workspace       = true
rust-version.workspace  = true
license.workspace       = true
authors.workspace       = true

[lib]
doctest         = false

[dependencies]
# Errors
errors                      = { workspace = true }

# Serialization for KdfParams and WrappedKey types.
serde                       = { workspace = true }
serde_json                  = { workspace = true }

[dev-dependencies]
